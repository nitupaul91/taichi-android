package taichi.walking.seniors.beginners.ui.styles

import taichi.walking.seniors.beginners.R
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.unit.sp

val MonaSansMedium = FontFamily(Font(R.font.mona_sans_medium))
val MonaSansRegular = FontFamily(Font(R.font.mona_sans_regular))
val MonaSansSemiBold = FontFamily(Font(R.font.mona_sans_semi_bold))
val MonaSansBoldWide = FontFamily(Font(R.font.mona_sans_bold_wide))
val MonaSansBold = FontFamily(Font(R.font.mona_sans_bold))

val MediumTextStyle = TextStyle(
    fontSize = 15.sp,
    fontFamily = MonaSansMedium
)

val VerySmallRegularTextStyle = TextStyle(
    fontSize = 12.sp,
    fontFamily = MonaSansRegular
)

val SmallRegularTextStyle = TextStyle(
    fontSize = 14.sp,
    fontFamily = MonaSansRegular
)

val SmallSemiBoldTextStyle = TextStyle(
    fontSize = 14.sp,
    fontFamily = MonaSansSemiBold
)

val SmallBoldTextStyle = TextStyle(
    fontSize = 14.sp,
    fontFamily = MonaSansBold
)

val MediumBoldWideTextStyle = TextStyle(
    fontSize = 18.sp,
    fontFamily = MonaSansBoldWide
)

val MediumBoldTextStyle = TextStyle(
    fontSize = 18.sp,
    fontFamily = MonaSansBold
)

val LargeBoldWideTextStyle = TextStyle(
    fontSize = 26.sp,
    fontFamily = MonaSansBoldWide
)

val VeryLargeBoldWideTextStyle = TextStyle(
    fontSize = 34.sp,
    fontFamily = MonaSansBoldWide
)
